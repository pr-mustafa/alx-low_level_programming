#include "main.h"
/* more headers goes there */

/**
 * add - Entry point
 *
 * @x: first parm
 * @y: second parm
 * Return: Always 0 (Success)
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
