#include "main.h"
#include <string.h>
/**
 *_strcat - change the value
 *@a: pointer of a
 *@b: pointer of b
 * Return: Always 0 (Success)
 */
char *_strcat(char *a, char *b)
{
	/* your code goes there */
	char *c = strcat(a, b);

	return (c);
}
