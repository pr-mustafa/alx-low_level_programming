#include "main.h"
#include <string.h>
/**
 *swap_int - change the value
 *@a: pointer of a
 *@b: pointer of b
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	/* your code goes there */
	return strlen(s);
}
