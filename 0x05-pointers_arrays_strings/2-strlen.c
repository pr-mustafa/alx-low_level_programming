#include "main.h"
#include <string.h>
/**
 *_strlen - return length of string
 *@s: pointer of s
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	/* your code goes there */
	return (strlen(s));
}
