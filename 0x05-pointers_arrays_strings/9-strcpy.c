#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 *_strcpy - change the value
 *@dest: pointer of a
 *@src: size of elements to be print
 * Return: Always 0 (Success)
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
